## Description
This application uses Github API to print users' repositories given their usernames. Repositories will be stored in the path `./Users/"USERNAME".txt` 

## How to use it
In the CLI, run the command `repofinder` and enter the user's Github username
